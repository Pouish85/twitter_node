form(class="form-new" action=`/comment/new/${tweet._id}` method="post")
    .form-title 
        h4 Repondre à 
            a(href=`/user/${tweet.author.username}`) @#{tweet.author.username} 
    .form-body 
        .form-item 
            textarea(name="message" placeholder="Tweeter votre réponse")
        if errors 
            ul
                each error in errors 
                    li(class="text-danger")= error 
        .form-item
            input(type="submit", value= tweet ? "Commenter" : "Tweeter" class="btn btn-primary")